[project]
name = "tfp"
version = "0.1.0"
description = "Timmy's Financial Planner"
requires-python = ">=3.10"

[project.scripts]
tfp = "tfp.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
